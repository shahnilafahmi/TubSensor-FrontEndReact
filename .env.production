# Backend origin used by the production build (GitHub Pages).
# The TubSensor page calls ${VITE_API_BASE}/api/... directly from the browser,
# so the Railway backend must send CORS headers allowing
# https://shahnilafahmi.github.io
VITE_API_BASE=https://iotsensor-production-9d9f.up.railway.app
